import{NextResponse}from"next/server";import{getDatabasePath,openReadonlyDatabase}from"../../../db/connection";import{JobObservationRepository}from"../../../server/job-observations/repository";export const dynamic="force-dynamic";export async function GET(){try{const db=openReadonlyDatabase(getDatabasePath());try{return NextResponse.json({freshness:new JobObservationRepository(db).freshness()});}finally{db.close()}}catch{return NextResponse.json({error:{code:"OBSERVATIONS_UNAVAILABLE",message:"관찰 이력을 읽을 수 없습니다."}},{status:500})}}
