import{NextResponse}from"next/server";import{openReadonlyDatabase}from"../../../../db/connection";import{JobObservationRepository}from"../../../../server/job-observations/repository";import{getJobsDatabasePath}from"../../../../server/runtime/public-demo-database";export const dynamic="force-dynamic";export async function GET(_:Request,{params}:{params:Promise<{jobId:string}>}){const{jobId}=await params;if(!/^[0-9a-z-]{3,100}$/iu.test(jobId))return NextResponse.json({error:{code:"JOB_ID_INVALID",message:"공고 ID가 올바르지 않습니다."}},{status:400});try{const db=openReadonlyDatabase(getJobsDatabasePath());try{return NextResponse.json(new JobObservationRepository(db).history(jobId));}finally{db.close()}}catch{return NextResponse.json({error:{code:"OBSERVATIONS_UNAVAILABLE",message:"관찰 이력을 읽을 수 없습니다."}},{status:500})}}
