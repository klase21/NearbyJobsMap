// @vitest-environment jsdom
import "@testing-library/jest-dom/vitest";import{cleanup,fireEvent,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it}from"vitest";import{FirstRunOnboarding,ONBOARDING_STORAGE_KEY,onboardingCompleted}from"../../components/onboarding/FirstRunOnboarding";
afterEach(()=>{cleanup();localStorage.clear();});
describe("first-run onboarding",()=>{it("opens on first use and explains all six steps",()=>{render(<FirstRunOnboarding/>);expect(screen.getByRole("dialog")).toBeInTheDocument();for(let i=0;i<5;i++)fireEvent.click(screen.getByRole("button",{name:i===0?"시작하기":"다음"}));expect(screen.getByText("준비되었습니다")).toBeInTheDocument();expect(screen.getByRole("link",{name:"수집관리로 이동"})).toHaveAttribute("href","/collection");});it("completes and stays closed after reload",()=>{render(<FirstRunOnboarding/>);fireEvent.click(screen.getByRole("button",{name:"건너뛰기"}));expect(localStorage.getItem(ONBOARDING_STORAGE_KEY)).toBe("dismissed");cleanup();render(<FirstRunOnboarding/>);expect(screen.queryByRole("dialog")).not.toBeInTheDocument();});it("reopens explicitly and manages focus/accessibility",()=>{localStorage.setItem(ONBOARDING_STORAGE_KEY,"completed");render(<FirstRunOnboarding forceOpen/>);const dialog=screen.getByRole("dialog");expect(dialog).toHaveAttribute("aria-modal","true");expect(screen.getByRole("button",{name:"시작하기"})).toHaveFocus();});it("treats stale versions as incomplete",()=>{localStorage.setItem(ONBOARDING_STORAGE_KEY,"v0");expect(onboardingCompleted(localStorage)).toBe(false);});});
