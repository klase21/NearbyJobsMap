// @vitest-environment jsdom
import{afterEach,describe,expect,it,vi}from"vitest";import{cleanup,fireEvent,render,screen}from"@testing-library/react";import"@testing-library/jest-dom/vitest";import{SavedViewsBar}from"../../components/saved-views/SavedViewsBar";import{DEFAULT_FILTERS}from"../../services/job-search";afterEach(()=>{cleanup();vi.restoreAllMocks()});describe("saved views bar",()=>{it("renders accessible save controls and applies a saved view",async()=>{const onApply=vi.fn();vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({views:[{id:"view_1",name:"관심 공고",filters:DEFAULT_FILTERS,isFavorite:true}]})}));render(<SavedViewsBar filters={DEFAULT_FILTERS} onApply={onApply}/>);expect(await screen.findByRole("option",{name:"★ 관심 공고"})).toBeInTheDocument();fireEvent.change(screen.getByLabelText("보기 선택"),{target:{value:"view_1"}});expect(onApply).toHaveBeenCalledWith(DEFAULT_FILTERS)});it("keeps the save action disabled for an invalid short name",()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({views:[]})}));render(<SavedViewsBar filters={DEFAULT_FILTERS} onApply={()=>{}}/>);fireEvent.change(screen.getByLabelText("현재 필터 이름"),{target:{value:"a"}});expect(screen.getByRole("button",{name:"현재 필터 저장"})).toBeDisabled()})});
