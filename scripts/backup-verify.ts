import{resolveBackupPath,verifyBackup}from"../src/server/backups/backup-service";import{resolve}from"node:path";const i=process.argv.indexOf("--file"),file=i>=0?process.argv[i+1]:null;if(!file)throw new Error("--file is required");const result=verifyBackup(resolveBackupPath(resolve("data/backups"),file));console.log(`verified=${result.manifest.file} sha256=${result.manifest.sha256}`);
